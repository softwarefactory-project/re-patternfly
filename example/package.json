{
  "name": "re-patternfly-seed",
  "version": "1.0.0",
  "description": "A starter project for re-patternfly",
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w",
    "serve": "parcel serve ./src/index.html",
    "dist": "parcel build ./src/index.html --public-url /",
    "test": "npm run build && jest --verbose --testMatch **/tests/*.bs.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@softwarefactory-project/re-patternfly": "file:..",
    "@patternfly/react-core": "^4.50.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "reason-react": "^0.9.1"
  },
  "devDependencies": {
    "@glennsl/bs-jest": "^0.5.1",
    "bs-platform": "^8.2.0"
  },
  "jest": {
    "moduleNameMapper": {
      "\\.(css|less)$": "<rootDir>/tests/styleMock.js"
    }
  }
}
